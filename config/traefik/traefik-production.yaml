# GENERATED: DO NOT EDIT. Source: standards/service-registry.yaml
# Generated for production environment (mTLS: true, TLS: true)
global:
  checkNewVersion: false
  sendAnonymousUsage: false
entryPoints:
  web:
    address: :80
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
  metrics:
    address: :8099
  websecure:
    address: :443
ping:
  entryPoint: web
metrics:
  prometheus:
    entryPoint: metrics
    addEntryPointsLabels: true
    addRoutersLabels: true
    addServicesLabels: true
tracing:
  otlp:
    grpc:
      endpoint: otel-collector:4317
      insecure: true
certificatesResolvers:
  letsencrypt:
    acme:
      email: admin@example.com
      storage: /etc/traefik/acme.json
      httpChallenge:
        entryPoint: web
accessLog:
  format: common
  fields:
    headers:
      defaultMode: drop
      names:
        X-Forwarded-For: keep
        X-Real-Ip: keep
        User-Agent: keep
        Origin: keep
log:
  level: WARN
providers:
  file:
    filename: /etc/traefik/dynamic.yaml
    watch: true
api:
  dashboard: false
