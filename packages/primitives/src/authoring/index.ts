// Headless primitives for control drafting (ADR-0008's Author seam), shared by
// the two authoring UIs. See drafts.ts for why this lives here rather than
// twice.
export * from './drafts';
export * from './accept';
