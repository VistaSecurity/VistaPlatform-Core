// The connector registry, for UIs that present the catalogue of external
// systems the platform can integrate with.
//
// Everything here is GENERATED from standards/connectors.yaml — see
// registry.gen.ts. Import it as `@vistasecurity/primitives/connectors` rather
// than hand-keeping a list of platform names beside the page, which is the
// drift this replaces.
export * from './registry.gen';
